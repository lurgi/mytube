mixin video(video)
    a(href=`/videos/${video._id}`).video_container
        div.video_thumbnail
        div.video_detail
            div.video_info
                span.video_title=video.title
                div
                    small #{video.owner.username}
                    small •
                    small #{video.views} 조회수